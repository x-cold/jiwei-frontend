<!DOCTYPE>
<html>
{{{include ('./head')}}}
<body>
    <!-- header -->
    {{{include ('./header')}}}

    <!-- main block -->
    <section id="wrapper">
        {{{block ("body")}}}
    </section>

    <!-- aside-box -->
    <section class="aside-box">
    </section>

    <!-- footer -->
    {{{include ('./footer')}}}
</body>
</html>