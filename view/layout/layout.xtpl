<!DOCTYPE>
<html>
{{{include ('./head')}}}
<body>
    <!-- header -->
    {{{include ('./header')}}}

    <!-- main block -->
    <section id="wrapper">
        {{{block ("wrap")}}}
    </section>

    <!-- aside-box -->
    <section class="aside-box">
    {{{block ("aside")}}}
    </section>

    <!-- footer -->
    {{{include ('./footer')}}}
</body>
</html>